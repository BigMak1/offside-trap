# Local Claude Code config & skills — not part of the game
.claude/

# Dependencies
node_modules/

# Deploy / build / QA artifacts
.vercel/
.playwright-mcp/
assets/_build/_*.png
# packaged builds (itch.io zip, store covers) — regenerable, kept out of git
dist/

# Large input media dropped in the task folder — not part of the shipped game
.ai/*.mp3
.ai/*.wav
.ai/*.ogg

# Temp scratch & screenshots (keep the repo clean for agents reading it)
/*.png
ot-*.png
page-*.png
screenshot*.png
*.log
*.tmp

# OS
.DS_Store
Thumbs.db
